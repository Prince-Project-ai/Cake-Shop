import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    fieldError: {},
}

const formValidation = createSlice({
    name: "formValidation",
    initialState,
    reducers: {
        setFieldError: (state, action) => {
            const
        }
    }

});